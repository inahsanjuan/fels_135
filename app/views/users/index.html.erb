<% provide :title, t("admin.allusers") %>
<h1><%= t "admin.allusers" %></h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user %>
      <%= link_to user.name, "#" %>
    </li></br>
  <% end %>
</ul>
<%= will_paginate @users %>
